server:
  port: 8080

spring:
  datasource:
    #url: jdbc:mysql://localhost:3306/bbs?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    url: jdbc:mysql://root:onZVQcqRNzaGgCdztrvJqodxyryEofyg@mysql.railway.internal:3306/railway
    username: root
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver
  # PostgreSQL profile for deployment
  # Activate with: spring.profiles.active=prod

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: bbs-system-jwt-secret-key-2024-must-be-at-least-256-bits-long-for-hs256
  expiration: 7200000  # 2 hours in milliseconds

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
